"use client"
import React from 'react'
import PostCard from '@/components/PostCard'

const page = ({ props }) => {
  return (
    <div>
      <h1>{props.title}</h1>
    </div>
  )
}

export default page
